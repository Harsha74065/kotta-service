{
  "name": "service-company-app",
  "version": "1.0.0",
  "description": "ServiceOps - Home appliance repair and service management platform",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "nodemon server/index.js",
    "client": "cd client && npm start",
    "start": "node server/index.js",
    "build": "cd client && npm install && npm run build",
    "install-all": "npm install && cd client && npm install",
    "render-build": "npm install && cd client && npm install && npm run build",
    "vercel-build": "cd client && npm install && npm run build"
  },
  "keywords": ["service", "repair", "management", "serviceops"],
  "author": "ServiceOps",
  "license": "ISC",
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "jsonwebtoken": "^9.0.2",
    "razorpay": "^2.9.6",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "nodemon": "^3.0.1"
  }
}
